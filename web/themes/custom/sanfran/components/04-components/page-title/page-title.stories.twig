{% stories page_title with { 
  title: 'AccuEdits/Components/Page Title'
} %}

  {% story page_title_default with {
    name: 'Default',
    args: {
      heading: 'Welcome to the Page Title component',
      callout: 'This is a text-only component',
      description: 'This is a detailed and wordy example for the description field.',
      ctaText: 'Click me!',
      ctaHref: 'https://www.sfhsa.org',
      background_colors: ['lime','red','orange','yellow','green','teal','blue','purple','magenta','gray']
    }
  } %}

    {% for key,background_color in background_colors %}
      {{ include('sanfran:page-title', {
        heading,
        callout,
        description,
        ctaText,
        ctaHref,
        background_color,
      }) }}
    {% endfor %}

  {% endstory %}

{% endstories %}
