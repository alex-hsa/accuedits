{% set classes = [
  'h' ~ level,
  class,
]|join(' ')|trim %}

<h{{ level }} class="{{ classes }}">
  {% if url %}
    {% include "sanfran:link" with {
      url,
      text,
      } %}
  {% else %}
    {{ text }}
  {% endif %}
</h{{ level }}>